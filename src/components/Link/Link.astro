---
interface Props {
  href: string;
  style?: string;
  openHere?: boolean;
}

const { href, openHere = false, style = "" } = Astro.props;
---

<a
  class=`underline ${style}`
  href={href}
  target={openHere ? undefined : "_blank"}><slot /></a
>
