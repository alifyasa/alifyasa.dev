---
interface Props {
    href: string;
    openHere?: boolean;
}

const { href, openHere = false } = Astro.props;
---

<a class="underline" href={href} target={openHere ? undefined : "_blank"}><slot /></a>
