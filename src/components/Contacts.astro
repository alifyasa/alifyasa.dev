---
import Link from "./Link/Link.astro";
import { GET } from "../pages/resume/contacts.json";

const contactsRequest = await GET();
const contacts: { name: string; url: string }[] = await contactsRequest.json();
---

<>
    <h2 class="text-xl font-semibold my-2">Contacts</h2>
    <div class="flex">
        {
            contacts.map((contact) => (
            <div class="flex border border-gray-200">
                <Link
                    style="px-4 py-2 block text-left w-full hover:bg-gray-200"
                    href={contact.url}
                >
                    {contact.name}
                </Link>
            </div>
            ))
        }
    </div>
</>
